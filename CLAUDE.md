# Out Loud — بلند بگو

اتاق تمرین مکالمه‌ی انگلیسی برای بزرگسالان، ساخته‌شده برای نادیا (ترسِ خودش
از حرف‌زدنِ بلادرنگ به انگلیسی/فرانسه، با وجود نوشتار/خواندنِ قوی). فرض
اصلی پروژه: قوی‌ترین پیش‌بینی‌کننده‌ی روان‌شدن در یک زبان «زمان صحبت‌کردن»
است، نه دستور زبان — پس اپ به‌جای تمرین گرامر، زمان حرف‌زدن واقعی و تصحیحِ
فوریِ بی‌صدا می‌دهد.

توضیح کامل در `README.md` است؛ این فایل فقط خلاصه‌ی سریع برای مراجعه است.

## تصمیم‌های طراحی — بدون درخواست صریح کاربر دوباره به بحث گذاشته نشوند

- تصحیح **فوری** بعد از هر نوبتِ کاربر نشان داده می‌شود، نه در جمع‌بندی پایانی
  — و همیشه در ریل فارسی کنار صفحه، نه در دهان طرف مقابل (طرف مقابل هرگز از
  نقش خارج نمی‌شود).
- رابط کاربری کاملاً دوزبانه است (چارچوب فارسی + مکالمه‌ی انگلیسی) — نه
  کاملاً انگلیسی، نه کاملاً فارسی.
- کتابخانه‌ی محتوا ترکیبی است: سناریوهای هدف‌دار (`content/scenarios.json` —
  ۶ کاری + ۶ روزمره، هرکدام هدف + دستور کار پنهان برای طرف مقابل) و موضوعات
  گفت‌وگوی آزاد بدون هدف (`content/topics.json`).
- بدون فریم‌ورک، بدون bundler — فایل‌های `.mjs` ساده، هم‌سبک با پروژه‌ی
  خواهر `News Studio`.

## معماری موتور (mock ⇄ claude)

پشت یک اینترفیس یکسان در `server/engine/index.mjs`، انتخاب با `ENGINE` در
`.env`:

- **`ENGINE=mock`** (پیش‌فرض فعلی) — بدون کلید API کار می‌کند؛ مکالمه از
  پیش نوشته‌شده، تصحیح قانون‌محور (خطاهای رایج فارسی‌زبان‌ها مثل «I am
  agree»)، جمع‌بندی از متن گفت‌وگو محاسبه می‌شود. `server/engine/mock.mjs`.
- **`ENGINE=claude`** — نیازمند `ANTHROPIC_API_KEY`، مدل `claude-opus-5`.
  `server/engine/claude.mjs` کلاینت Anthropic را فقط در اولین فراخوانی واقعی
  می‌سازد (import امن حتی بدون کلید). **هنوز در برابر API واقعی تست نشده** —
  وقتی کاربر کلید گرفت، `ENGINE=claude` را در `.env` فعال کن و کل فلو را
  دوباره تست کن.

## نکته‌ی فنی مهم (باگ رفع‌شده، دوباره تکرار نشود)

در `public/app.css`: صفت `[hidden]` باید همه‌جا `display: none !important`
داشته باشد. هر کلاسی که خودش `display` ست می‌کند (مثلاً `.session-body
{ display: grid }`) با تخصیص یکسان، `hidden` را بی‌اثر می‌کند چون قاعده‌ی
UA در برابر قاعده‌ی نویسنده با تخصیص برابر می‌بازد.

## نقشه‌ی فایل‌ها

| مسیر | نقش |
|---|---|
| `server/index.mjs` | سرور `node:http` — بدون فریم‌ورک |
| `server/engine/index.mjs` | انتخاب‌گر موتور بر اساس `ENGINE` |
| `server/engine/mock.mjs` / `claude.mjs` | دو پیاده‌سازیِ همان اینترفیس |
| `server/prompts/` | پرامپت سیستمی سه بخش: طرف مقابل، مربی (تصحیح)، جمع‌بندی |
| `content/scenarios.json` | ۱۲ سناریوی هدف‌دار |
| `content/topics.json` | ۱۲ موضوع گفت‌وگوی آزاد |
| `public/index.html`, `app.js`, `app.css` | رابط کاربری، بدون build step |
| `public/lib/store.mjs` | بانک عبارات با سیستم Leitner در `localStorage` |
| `public/lib/speech.mjs` | تشخیص گفتار/خواندن متن (Web Speech API؛ فallback خودکار به ورودی متنی در مرورگرهای دیگر) |
| `public/lib/ribbon.mjs` | نوار «چند درصد را تو گفتی» |

## دستورها

```
npm install
npm run setup:fonts   # یک‌بار — دانلود فونت‌های لاتین
cp .env.example .env
npm start             # http://localhost:5173
```

هیچ حساب کاربری، هیچ دیتابیس — همه‌چیز در `localStorage` مرورگر می‌ماند.
کلید API فقط سمت سرور خوانده می‌شود، هرگز به مرورگر نمی‌رسد.
